load("@io_bazel_rules_go//go:def.bzl", "go_binary")
load("@qrysm//tools/go:def.bzl", "go_library")
load("//tools:qrysm_image.bzl", "qrysm_image_upload")

go_library(
    name = "qrysmctl_lib",
    srcs = ["main.go"],
    importpath = "github.com/theQRL/qrysm/cmd/qrysmctl",
    visibility = ["//visibility:private"],
    deps = [
        "//cmd/qrysmctl/checkpointsync",
        "//cmd/qrysmctl/db",
        "//cmd/qrysmctl/p2p",
        "//cmd/qrysmctl/testnet",
        "//cmd/qrysmctl/validator",
        "//cmd/qrysmctl/weaksubjectivity",
        "@com_github_sirupsen_logrus//:logrus",
        "@com_github_urfave_cli_v2//:cli",
    ],
)

go_binary(
    name = "qrysmctl",
    embed = [":qrysmctl_lib"],
    gotags = ["noMainnetGenesis"],
    visibility = ["//visibility:public"],
)

qrysm_image_upload(
    name = "push_oci_image",
    binary = ":qrysmctl",
    entrypoint = ["/qrysmctl"],
    repository = "index.docker.io/qrledger/qrysm",
    symlinks = {
        # Backwards compatiability for images that depended on the old filepath.
        "/app/cmd/qrysmctl/qrysmctl": "/qrysmctl",
    },
    tags = ["qrysmctl-manual"],
)
