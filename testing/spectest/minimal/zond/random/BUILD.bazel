load("@qrysm//tools/go:def.bzl", "go_test")

go_test(
    name = "random_test",
    size = "small",
    srcs = ["random_test.go"],
    data = glob(
        ["*.yaml"],
        allow_empty = True,
    ) + [
        #"@consensus_spec_tests_minimal//:test_data",
    ],
    qrl_network = "minimal",
    tags = ["spectest"],
)
